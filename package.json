{
  "name": "atlasai",
  "private": true,
  "packageManager": "npm@11.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "concurrently": "^10.0.4",
    "turbo": "^2",
    "typescript": "^6.0.3",
    "wait-on": "^9.1.0"
  }
}
